# frontend/.env.production
# Set this to your live backend URL once you deploy the backend
# (e.g. on Render.com — see README for instructions)
# Vite exposes variables prefixed with VITE_ to the frontend build.

VITE_API_URL=https://your-backend.onrender.com
